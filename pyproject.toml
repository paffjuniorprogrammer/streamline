[project]
name = "streamline"
version = "0.1.0"
description = "Authorized media streaming interface"
requires-python = ">=3.10"
dependencies = [
    "Flask>=3.0,<4.0",
]

[tool.vercel]
entrypoint = "movie.py"
